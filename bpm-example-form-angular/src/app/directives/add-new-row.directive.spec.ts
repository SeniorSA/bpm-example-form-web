import { AddNewRowDirective } from './add-new-row.directive';

describe('AddNewRowDirective', () => {
  it('should create an instance', () => {
    const directive = new AddNewRowDirective();
    expect(directive).toBeTruthy();
  });
});
